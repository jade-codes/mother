# Rust formatting - stable options only
edition = "2021"

# Basic formatting rules
max_width = 100
tab_spaces = 4
use_small_heuristics = "Default"

# Imports (stable options only)
reorder_imports = true
reorder_modules = true
